<!DOCTYPE html>
<html lang="pt">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">    <title>Registar Resultado - Peladas das Quintas Feiras</title>
  <link rel="stylesheet" href="/style.css?v=20250101">
  <link rel="icon" type="image/svg+xml" href="/logo.svg">
  <link rel="shortcut icon" href="/logo.svg">
</head>
<body class="main-bg">  
<%- include('partials/header', { user: user }) %>

  <main class="container">
    <div class="game-header">
      <h2 class="section-title">⚽ Registar Novo Resultado</h2>
      <p class="section-subtitle">Introduz os dados do jogo</p>
    </div>

    <form method="POST" action="/jogos" class="game-form-modern">
      <div class="game-date-card">
        <label for="data">📅 Data do Jogo</label>
        <input type="date" id="data" name="data" required class="input-date-modern">
      </div>      <div class="teams-grid">
        <div class="team-card team-blue">
          <div class="team-header">
            <h3>🔵 Equipa 1</h3>
          </div>
          <div class="team-goals">
            <label for="equipa1_golos">Golos Marcados</label>
            <input type="number" id="equipa1_golos" name="equipa1_golos" min="0" value="0" required class="input-goals">
          </div>
          <div class="team-players">
            <label class="players-label">Selecionar Jogadores</label>
            <div class="players-grid">
              <% if (jogadores && jogadores.length > 0) { %>
                <% jogadores.forEach(jogador => { %>
                  <label class="player-checkbox-modern">
                    <input type="checkbox" name="equipa1" value="<%= jogador.id %>">
                    <span class="checkbox-custom"></span>
                    <span class="player-name"><%= jogador.nome %></span>
                  </label>
                <% }) %>
              <% } %>
            </div>
          </div>
        </div>
        
        <div class="vs-divider">
          <span>VS</span>
        </div>
        
        <div class="team-card team-red">
          <div class="team-header">
            <h3>🔴 Equipa 2</h3>
          </div>
          <div class="team-goals">
            <label for="equipa2_golos">Golos Marcados</label>
            <input type="number" id="equipa2_golos" name="equipa2_golos" min="0" value="0" required class="input-goals">
          </div>
          <div class="team-players">
            <label class="players-label">Selecionar Jogadores</label>
            <div class="players-grid">
              <% if (jogadores && jogadores.length > 0) { %>
                <% jogadores.forEach(jogador => { %>
                  <label class="player-checkbox-modern">
                    <input type="checkbox" name="equipa2" value="<%= jogador.id %>">
                    <span class="checkbox-custom"></span>
                    <span class="player-name"><%= jogador.nome %></span>
                  </label>
                <% }) %>
              <% } %>
            </div>
          </div>
        </div>
      </div>      <div class="form-actions-modern">
        <button type="submit" class="btn btn-success">
          <span>✅</span> Registar Resultado
        </button>
        <a href="/" class="btn btn-cancel">
          <span>❌</span> Cancelar
        </a>
      </div>
    </form>
  </main>

<%- include('partials/footer') %>
</body>
</html>