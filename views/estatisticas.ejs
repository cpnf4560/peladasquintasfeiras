<!DOCTYPE html>
<html>
<head>
  <title>Estatísticas - Peladas das Quintas Feiras</title>
  <link rel="stylesheet" href="/style.css?v=<%= Date.now() %>">
  <link rel="icon" type="image/svg+xml" href="/logo.svg">
  <link rel="shortcut icon" href="/logo.svg">
</head>
<body class="main-bg">
  <%- include('partials/header', { user: user, activePage: 'estatisticas' }) %>

  <main class="container">
    <h2 class="section-title">Estatísticas dos Jogadores</h2>

    <div class="stats-filters">
      <div class="filter-group">
        <label for="ano">Ano:</label>
        <select id="ano" onchange="updateEstats()">
          <option value="2024" <%= anoSelecionado === '2024' ? 'selected' : '' %>>2024</option>
          <option value="2025" <%= anoSelecionado === '2025' ? 'selected' : '' %>>2025</option>
          <option value="2026" <%= anoSelecionado === '2026' ? 'selected' : '' %>>2026</option>
          <option value="2027" <%= anoSelecionado === '2027' ? 'selected' : '' %>>2027</option>
        </select>
      </div>
      <div class="filter-group">
        <label for="mes">Mês:</label>
        <select id="mes" onchange="updateEstats()">
          <option value="" <%= mesSelecionado === '' ? 'selected' : '' %>>Todos</option>
          <option value="1" <%= mesSelecionado === '1' ? 'selected' : '' %>>Janeiro</option>
          <option value="2" <%= mesSelecionado === '2' ? 'selected' : '' %>>Fevereiro</option>
          <option value="3" <%= mesSelecionado === '3' ? 'selected' : '' %>>Março</option>
          <option value="4" <%= mesSelecionado === '4' ? 'selected' : '' %>>Abril</option>
          <option value="5" <%= mesSelecionado === '5' ? 'selected' : '' %>>Maio</option>
          <option value="6" <%= mesSelecionado === '6' ? 'selected' : '' %>>Junho</option>
          <option value="7" <%= mesSelecionado === '7' ? 'selected' : '' %>>Julho</option>
          <option value="8" <%= mesSelecionado === '8' ? 'selected' : '' %>>Agosto</option>
          <option value="9" <%= mesSelecionado === '9' ? 'selected' : '' %>>Setembro</option>
          <option value="10" <%= mesSelecionado === '10' ? 'selected' : '' %>>Outubro</option>
          <option value="11" <%= mesSelecionado === '11' ? 'selected' : '' %>>Novembro</option>
          <option value="12" <%= mesSelecionado === '12' ? 'selected' : '' %>>Dezembro</option>
        </select>
      </div>
      <div class="filter-group">
        <label for="ordenacao">Ordenar por:</label>
        <select id="ordenacao" onchange="updateEstats()">
          <option value="pontos" <%= ordenacaoSelecionada === 'pontos' ? 'selected' : '' %>>Pontos</option>
          <option value="percentagem" <%= ordenacaoSelecionada === 'percentagem' ? 'selected' : '' %>>% de Vitórias</option>
        </select>      </div>
    </div>

    <!-- Curiosidades/Observações -->
    <% if (curiosidades && curiosidades.length > 0) { %>
      <div class="curiosidades-section">
        <h3 class="curiosidades-title">🔍 Curiosidades & Observações</h3>
        <div class="curiosidades-grid">
          <% curiosidades.forEach(curiosidade => { %>
            <div class="curiosidade-card card-visual">
              <div class="curiosidade-icon"><%= curiosidade.icone %></div>
              <div class="curiosidade-content">
                <h4><%= curiosidade.titulo %></h4>
                <p><%= curiosidade.texto %></p>
              </div>
            </div>
          <% }) %>
        </div>
      </div>
    <% } %>    <!-- Análise de Duplas -->
    <% var duplasSafe = typeof duplas !== 'undefined' && duplas ? duplas : { melhorVitorias: [], piorVitorias: [], menosJogos: [] }; %>
    <% if (duplasSafe.melhorVitorias && duplasSafe.melhorVitorias.length > 0) { %>
      <div class="duplas-section">
        <h3 class="duplas-title">👥 Análise de Duplas</h3>
        <p class="duplas-subtitle"><em>Duplas que jogaram pelo menos 3 jogos juntas</em></p>
        
        <!-- TOP 3 - Melhor % Vitórias -->
        <div class="dupla-categoria">
          <h4 class="dupla-categoria-header">
            <span class="dupla-icon">🏆</span>
            TOP 3 - Duplas com mais % de vitórias
          </h4>
          <div class="duplas-top3-grid">
            <% duplasSafe.melhorVitorias.forEach((dupla, idx) => { %>
              <div class="dupla-card <%= idx === 0 ? 'destaque-ouro' : idx === 1 ? 'destaque-prata' : 'destaque-bronze' %>">
                <div class="dupla-posicao"><%= idx + 1 %>º</div>
                <h5 class="dupla-nomes"><%= dupla.jogador1 %> & <%= dupla.jogador2 %></h5>
                <div class="dupla-stat-principal">
                  <span class="stat-valor-grande"><%= dupla.percentagem_vitorias %>%</span>
                  <span class="stat-label-pequeno">de vitórias</span>
                </div>
                <div class="dupla-stats-linha">
                  <span class="stat-badge wins"><%= dupla.vitorias %>V</span>
                  <span class="stat-badge draws"><%= dupla.empates || 0 %>E</span>
                  <span class="stat-badge losses"><%= dupla.derrotas || 0 %>D</span>
                </div>
                <p class="dupla-info-jogos"><%= dupla.jogos_juntos %> jogos juntos</p>
              </div>
            <% }) %>
          </div>
        </div>

        <!-- TOP 3 - Pior % Vitórias -->
        <div class="dupla-categoria">
          <h4 class="dupla-categoria-header">
            <span class="dupla-icon">💩</span>
            TOP 3 - Duplas com mais % de derrotas
          </h4>
          <div class="duplas-top3-grid">
            <% duplasSafe.piorVitorias.forEach((dupla, idx) => { %>
              <div class="dupla-card destaque-vermelho">
                <div class="dupla-posicao"><%= idx + 1 %>º</div>
                <h5 class="dupla-nomes"><%= dupla.jogador1 %> & <%= dupla.jogador2 %></h5>
                <div class="dupla-stat-principal">
                  <span class="stat-valor-grande"><%= dupla.percentagem_vitorias %>%</span>
                  <span class="stat-label-pequeno">de vitórias</span>
                </div>
                <div class="dupla-stats-linha">
                  <span class="stat-badge wins"><%= dupla.vitorias %>V</span>
                  <span class="stat-badge draws"><%= dupla.empates || 0 %>E</span>
                  <span class="stat-badge losses"><%= dupla.derrotas || 0 %>D</span>
                </div>
                <p class="dupla-info-jogos"><%= dupla.jogos_juntos %> jogos juntos</p>
              </div>
            <% }) %>
          </div>
        </div>

        <!-- TOP 3 - Menos Jogos Juntos -->
        <div class="dupla-categoria">
          <h4 class="dupla-categoria-header">
            <span class="dupla-icon">🆕</span>
            TOP 3 - Duplas com menos jogos juntos
          </h4>
          <div class="duplas-top3-grid">
            <% duplasSafe.menosJogos.forEach((dupla, idx) => { %>
              <div class="dupla-card destaque-azul">
                <div class="dupla-posicao"><%= idx + 1 %>º</div>
                <h5 class="dupla-nomes"><%= dupla.jogador1 %> & <%= dupla.jogador2 %></h5>
                <div class="dupla-stat-principal">
                  <span class="stat-valor-grande"><%= dupla.jogos_juntos %></span>
                  <span class="stat-label-pequeno">jogos juntos</span>
                </div>
                <div class="dupla-stats-linha">
                  <span class="stat-badge wins"><%= dupla.vitorias %>V</span>
                  <span class="stat-badge draws"><%= dupla.empates || 0 %>E</span>
                  <span class="stat-badge losses"><%= dupla.derrotas || 0 %>D</span>
                </div>
                <p class="dupla-info-jogos"><%= dupla.percentagem_vitorias %>% de vitórias</p>
              </div>
            <% }) %>
          </div>
        </div>
      </div>
    <% } %>

    <!-- MVP Mensal -->
    <% if (mesSelecionado && mvpMensais && mvpMensais.length > 0) { %>
      <div class="mvp-section">
        <h3 class="mvp-title">🏆 MVP<%= mvpMensais.length > 1 ? 's' : '' %> do Mês</h3>
        <% if (mvpMensais.length > 1) { %>
          <p class="mvp-empate"><em>Empate no MVP! <%= mvpMensais.length %> jogadores empatados com <%= mvpMensais[0].pontos %> pontos</em></p>
        <% } %>
        <div class="mvp-cards">
          <% mvpMensais.forEach(mvp => { %>
            <div class="mvp-card">
              <div class="mvp-info">
                <h4><%= mvp.nome %></h4>
                <p><strong><%= mvp.pontos %> pontos</strong> | <%= mvp.jogos %>/<%= totalJogosMes %> jogos (<%= Math.round((mvp.jogos/totalJogosMes)*100) %>%)</p>
                <p><%= mvp.vitorias %>V <%= mvp.empates %>E <%= mvp.derrotas %>D | +<%= mvp.diferenca_golos %> diferença de golos</p>
              </div>
            </div>
          <% }) %>
        </div>        <p class="mvp-criteria"><em>Critério: Mínimo <%= minimoJogosParaMVP %> jogos (75% dos jogos do mês)</em></p>
      </div>
    <% } else if (mesSelecionado && totalJogosMes > 0 && (!mvpMensais || mvpMensais.length === 0)) { %>
      <div class="mvp-section">
        <div class="alert alert-warning">
          <h3>⚠️ MVP do Mês</h3>
          <p>Nenhum jogador qualificado para MVP (mínimo <%= minimoJogosParaMVP %> jogos de <%= totalJogosMes %> total)</p>
        </div>
      </div>
    <% } %><!-- Critérios de Classificação -->
    <div class="classification-criteria">
      <h4>📋 Critérios de Classificação <span style="color: #f9b233;">(Ordenação atual: <%= ordenacaoSelecionada === 'percentagem' ? '% de Vitórias' : 'Pontos' %>)</span></h4>
      <% if (ordenacaoSelecionada === 'percentagem') { %>
        <ol>
          <li><strong>% de Vitórias</strong> (critério principal)</li>
          <li><strong>Diferença de golos</strong> (em caso de empate de %)</li>
          <li><strong>Golos marcados</strong> (em caso de empate de diferença)</li>
          <li><strong>Número de presenças</strong> (em caso de empate de golos)</li>
        </ol>
      <% } else { %>
        <ol>
          <li><strong>Pontos:</strong> Vitória = 3pts, Empate = 1pt, Derrota = 0pts</li>
          <li><strong>Diferença de golos</strong> (em caso de empate de pontos)</li>
          <li><strong>Golos marcados</strong> (em caso de empate de diferença)</li>
          <li><strong>Número de presenças</strong> (em caso de empate de golos)</li>
        </ol>
      <% } %>
      <% if (mesSelecionado) { %>
        <p><strong>MVP Mensal:</strong> Jogador(es) com mais pontos tendo participado em pelo menos 75% dos jogos do mês</p>
      <% } %>
    </div>

    <table class="stats-table">
      <thead>
        <tr>
          <th>Pos</th>
          <th>Jogador</th>
          <th>Pontos</th>
          <th>Jogos</th>
          <th>Vitórias</th>
          <th>Empates</th>
          <th>Derrotas</th>
          <th>% Vitórias</th>
          <th>Golos Marcados</th>
          <th>Golos Sofridos</th>
          <th>Diferença</th>
        </tr>
      </thead>
      <tbody>
        <% if (estatisticas && estatisticas.length > 0) { %>
          <% estatisticas.forEach((stat, index) => { %>
            <tr class="<%= mvpMensais && mvpMensais.some(mvp => mvp.id === stat.id) ? 'mvp-row' : '' %>">
              <td class="position"><%= index + 1 %>º</td>
              <td class="player-name">
                <%= stat.nome %>
                <% if (mvpMensais && mvpMensais.some(mvp => mvp.id === stat.id)) { %>
                  <span class="mvp-badge">🏆 MVP</span>
                <% } %>
              </td>
              <td class="stat-points"><strong><%= stat.pontos %></strong></td>
              <td><%= stat.jogos %></td>
              <td class="stat-wins"><%= stat.vitorias %></td>
              <td class="stat-draws"><%= stat.empates %></td>
              <td class="stat-losses"><%= stat.derrotas %></td>
              <td class="stat-percentage"><%= stat.percentagem_vitorias %>%</td>
              <td class="stat-goals-for"><%= stat.golos_marcados %></td>
              <td class="stat-goals-against"><%= stat.golos_sofridos %></td>
              <td class="stat-goal-diff <%= stat.diferenca_golos >= 0 ? 'positive' : 'negative' %>">
                <%= stat.diferenca_golos >= 0 ? '+' : '' %><%= stat.diferenca_golos %>
              </td>
            </tr>
          <% }) %>
        <% } else { %>
          <tr>
            <td colspan="11" style="text-align: center;">Nenhuma estatística encontrada para o período selecionado</td>
          </tr>
        <% } %>
      </tbody>
    </table>
  </main>
  <script>
  function updateEstats() {
    const ano = document.getElementById('ano').value;
    const mes = document.getElementById('mes').value;
    const ordenacao = document.getElementById('ordenacao').value;
    const url = `/estatisticas?ano=${ano}${mes ? '&mes=' + mes : ''}${ordenacao ? '&ordenacao=' + ordenacao : ''}`;
    window.location.href = url;
  }
  </script>
</body>
</html>